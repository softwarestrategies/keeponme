-- Example migration: Add phone number to users
-- This demonstrates how to add new columns

-- Uncomment when you actually want to add this feature:
-- ALTER TABLE users ADD COLUMN IF NOT EXISTS phone_number VARCHAR(20);
-- CREATE INDEX IF NOT EXISTS idx_users_phone ON users(phone_number);
-- COMMENT ON COLUMN users.phone_number IS 'Optional phone number for user';

-- This file serves as a template for future migrations
-- When you need to add/modify schema:
-- 1. Copy this file and rename to V2__description.sql (increment version)
-- 2. Add your DDL statements
-- 3. Flyway will automatically apply it on next startup
